{% extends 'back/index.html.twig' %}

{% block title %}Hello JeuController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 1000px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
        .kkkk {
            color: #fff;
            background-color: #FFE436;
            border-color: #FFE436;
            font-size: 20px;
            padding: 3px;
        }
    </style>
    <!-- stat -->

    <div class="app-content" style="text-align: center;margin-left: 10%;">
        <div class="side-app">
            <div id="div_chart"></div>

            <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
            <script type="text/javascript">
                {{ gc_draw(piechart, 'div_chart') }}
            </script>
        </div>
    </div>
    <!-- endstat -->
    <div class="example-wrapper">
        <main>

            <div class="container mt-5 mb-5">
                <div class="d-flex justify-content-between mb-3">
                <div class="row g-2">
                    {%  for j in jeu %}
                        <div class="col-md-3">
                            <div class="card p-2 py-3 text-center">
                                <div class="img mb-2">

                                </div>
                                <h3 > <p style="color:#1E90FF">Score :</p></h3><h5 class="mb-0">{{j.getScoreJeu()}}</h5> <h3><p style="color:#1E90FF">Joueur numéro :</p></h3> <small>{{ j.getIdUser()}}</small>
                                <div class="ratings mt-2"> <i class="fa fa-star"></i> <i class="fa fa-star"></i> <i class="fa fa-star"></i> <i class="fa fa-star"></i> </div>
                                <div class="mt-4 apointment">
                                    <button class="kkkk" > <a href="{{ path('s',{'id':j.getIdJeu()})}}">Delete </a></button>

                                </div>
                            </div>

                        </div>

                    {% endfor %}


                </div>
        </main>
    </div>
{% endblock %}

