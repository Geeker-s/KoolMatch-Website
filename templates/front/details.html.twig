{% extends 'base.html.twig' %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 1000px; width: 100%; font: 16px/0.7 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }

    </style>

    <div class="example-wrapper">
    <div class="container">
        <div class="row">
            <div></div>

            <div id="content" >
                <h1>{{recette.getNomRecette()}}</h1>
                <h3> Description : </h3>  <h2>{{ recette.getDescriptionRecette()}}</h2>
                <h3 >Catégorie :</h3> <h2>{{recette.getCategorieRecette()}}</h2>
                <h3> Durée: </h3> <h2>{{ recette.getDureeRecette()}}</h2>
                <!-- Qr code -->
                <div class="shipping-address-form">
                    <img src="{{ qr_code_url(myurl) }}" alt="myqr" style="margin-left: auto;margin-right: auto" class="img-responsive move">
                </div>
    </div>
    </div>
    </div>
{% endblock %}