{% extends 'base.html.twig' %}
{% block title %}Hello TestController!{% endblock %}
{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 1000px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
<div class="example-wrapper">
<main>
    
<div class="container mt-5 mb-5">
    <div class="d-flex justify-content-between mb-3"> <span>Restaurants</span> <button class="butt"><a href="{{ path('reservation_app') }}">Ajouter Reservation</a></button> </div>
    <div class="row g-2">
    {% for e in Reservation %}
      
            
       <div class="card">
                    <div class="card-body">
                      <h5 class="card-title">Reservations</h5>
                      <p class="card-text">{{e.nbplaceReservation}}</p>
                      <p class="card-text">{{e.dateReservation.format('d/m/Y')}}</p>
                    
                    </div>
                    <img src="{{e.image}}" class="card-img-bottom" alt="...">






                
             </div>
        {% endfor %}
        
</div>
</main>
</div>
{% endblock %}